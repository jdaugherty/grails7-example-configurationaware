setting.some.property="FOO"
// This will only work because the class is defined in the plugin instead of the app so it's already compiled
another.property=com.example.TestOption.OPTION1

closureSyntax {
    propertyTest=com.example.TestOption.OPTION2
}